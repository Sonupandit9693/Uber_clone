import { Text, View } from "react-native";

import { SafeAreaView } from "react-native-safe-area-context";

const SignOut=()=>{
    return(
        <SafeAreaView>
            <Text>Sign Out in page</Text>
        </SafeAreaView>
    )
}

export default SignOut;